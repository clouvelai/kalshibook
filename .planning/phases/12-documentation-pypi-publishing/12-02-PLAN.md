---
phase: 12-documentation-pypi-publishing
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - sdk/docs/getting-started.md
  - sdk/docs/authentication.md
  - sdk/docs/examples/orderbook.md
  - sdk/docs/examples/markets.md
  - sdk/docs/examples/candles.md
  - sdk/docs/examples/events.md
  - sdk/docs/examples/deltas.md
  - sdk/docs/examples/trades.md
  - sdk/docs/examples/settlements.md
  - sdk/docs/examples/dataframes.md
  - sdk/README.md

must_haves:
  truths:
    - "A new user can read Getting Started and make their first API call"
    - "A user can learn all authentication methods from the Authentication guide"
    - "Every endpoint has at least one complete code example"
    - "README.md on PyPI provides install, quickstart, and links to full docs"
  artifacts:
    - path: "sdk/docs/getting-started.md"
      provides: "Getting Started guide (DOCS-01)"
      contains: "pip install kalshibook"
    - path: "sdk/docs/authentication.md"
      provides: "Authentication guide (DOCS-02)"
      contains: "from_env"
    - path: "sdk/docs/examples/orderbook.md"
      provides: "Orderbook code examples (DOCS-04)"
      contains: "get_orderbook"
    - path: "sdk/docs/examples/markets.md"
      provides: "Market code examples (DOCS-04)"
      contains: "list_markets"
    - path: "sdk/docs/examples/candles.md"
      provides: "Candle code examples (DOCS-04)"
      contains: "get_candles"
    - path: "sdk/docs/examples/events.md"
      provides: "Event code examples (DOCS-04)"
      contains: "list_events"
    - path: "sdk/docs/examples/deltas.md"
      provides: "Delta pagination examples (DOCS-04)"
      contains: "list_deltas"
    - path: "sdk/docs/examples/trades.md"
      provides: "Trade pagination examples (DOCS-04)"
      contains: "list_trades"
    - path: "sdk/docs/examples/settlements.md"
      provides: "Settlement examples (DOCS-04)"
      contains: "list_settlements"
    - path: "sdk/docs/examples/dataframes.md"
      provides: "DataFrame conversion examples (DOCS-04)"
      contains: "to_df"
    - path: "sdk/README.md"
      provides: "Expanded README for PyPI"
      contains: "pip install kalshibook"
  key_links:
    - from: "sdk/docs/getting-started.md"
      to: "sdk/docs/authentication.md"
      via: "cross-link"
      pattern: "authentication.md"
    - from: "sdk/docs/getting-started.md"
      to: "sdk/docs/examples/"
      via: "cross-link to examples"
      pattern: "examples/"

autonomous: true
---

<objective>
Write all hand-crafted documentation content: Getting Started guide, Authentication guide, code examples for every endpoint, and expanded README.

Purpose: Satisfies DOCS-01, DOCS-02, DOCS-04 requirements. Gives users the narrative docs needed to learn the SDK beyond the auto-generated API reference.
Output: 11 markdown files comprising the complete documentation content, plus an expanded README for PyPI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-documentation-pypi-publishing/12-RESEARCH.md
@.planning/phases/12-documentation-pypi-publishing/12-01-SUMMARY.md
@sdk/src/kalshibook/client.py
@sdk/src/kalshibook/models.py
@sdk/src/kalshibook/exceptions.py
@sdk/src/kalshibook/_pagination.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Getting Started and Authentication guides</name>
  <files>
    sdk/docs/getting-started.md
    sdk/docs/authentication.md
  </files>
  <action>
    Read `sdk/src/kalshibook/client.py` to understand the constructor, `from_env()`, sync vs async mode, and context manager usage. Read `sdk/src/kalshibook/exceptions.py` for error types.

    1. Create `sdk/docs/getting-started.md` (DOCS-01) covering:
       - Installation: `pip install kalshibook` and `pip install kalshibook[pandas]`
       - Getting an API key: direct users to the KalshiBook dashboard (kalshibook.io)
       - First query example: construct client, call `get_orderbook()`, print bids/asks
       - List markets example: `list_markets()` and iterate results
       - Convert to DataFrame: show `.to_df()` on a response
       - Next steps: link to Authentication guide, Examples, API Reference
       - Use mkdocs-material admonitions for tips/notes (e.g., `!!! tip` for pandas optional dependency)
       - Lead with sync examples. Show tabbed sync/async for the first query using `=== "Sync"` / `=== "Async"` tabs.

    2. Create `sdk/docs/authentication.md` (DOCS-02) covering:
       - API key format: keys start with `kb-`
       - Direct key construction: `KalshiBook(api_key="kb-...")`
       - Environment variable: `KalshiBook.from_env()` reads `KALSHIBOOK_API_KEY`
       - Context manager usage: `with KalshiBook("kb-...") as client:` / `async with`
       - Sync vs async mode: `KalshiBook("kb-...", sync=False)` for async
       - Credit tracking: `response.meta.credits_used`, `response.meta.credits_remaining`
       - Error handling: try/except with AuthenticationError, CreditsExhaustedError, RateLimitError
       - Security best practices: never hardcode keys, use env vars or .env files
  </action>
  <verify>
    - `sdk/docs/getting-started.md` exists and contains installation, first query, and DataFrame examples
    - `sdk/docs/authentication.md` exists and covers direct key, from_env, context manager, error handling
    - `cd sdk && uv run mkdocs build` succeeds without errors
  </verify>
  <done>Getting Started and Authentication guides written with complete, runnable code examples covering all auth methods and basic usage patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Write endpoint code examples and expand README</name>
  <files>
    sdk/docs/examples/orderbook.md
    sdk/docs/examples/markets.md
    sdk/docs/examples/candles.md
    sdk/docs/examples/events.md
    sdk/docs/examples/deltas.md
    sdk/docs/examples/trades.md
    sdk/docs/examples/settlements.md
    sdk/docs/examples/dataframes.md
    sdk/README.md
  </files>
  <action>
    Read `sdk/src/kalshibook/client.py` and `sdk/src/kalshibook/models.py` to get method signatures, parameter names, and return types for every endpoint.

    1. Create example pages in `sdk/docs/examples/`. Each page should have:
       - A brief intro explaining what the endpoint does
       - At least one complete, runnable code example (sync mode primary)
       - Parameter explanations where useful
       - Expected output shape (show what attributes are available)

       Content per page:
       - **orderbook.md**: `get_orderbook(ticker, timestamp)`, show bid/ask levels, price/quantity access, context about L2 orderbook reconstruction
       - **markets.md**: `list_markets()` with optional filters, `get_market(ticker)` for detail, show MarketSummary vs MarketDetail attributes
       - **candles.md**: `get_candles(ticker, start, end, interval)`, show OHLCV fields, interval parameter options
       - **events.md**: `list_events()` with filters, `get_event(event_ticker)`, explain event/market hierarchy
       - **deltas.md**: `list_deltas(ticker, start, end)`, show PageIterator iteration, explain orderbook delta records
       - **trades.md**: `list_trades(ticker, start, end)`, show PageIterator iteration, trade record fields
       - **settlements.md**: `list_settlements()`, `get_settlement(ticker)`, settlement record fields
       - **dataframes.md**: `.to_df()` on list responses (MarketsResponse, CandlesResponse, etc.), `.to_df()` on PageIterator, show pandas operations (filtering, grouping), mention `pip install kalshibook[pandas]`

    2. Expand `sdk/README.md` to serve as a mini getting-started for PyPI visitors:
       - Title and badges placeholder (PyPI version, Python versions, license)
       - One-paragraph description
       - Installation (pip and pip with pandas)
       - Quick start: 3-4 line example (construct client, call endpoint, print result)
       - Key features list (typed responses, sync/async, auto-pagination, DataFrames)
       - Link to full documentation site
       - Link to API reference
       - License: MIT

    3. Run `cd sdk && uv run mkdocs build --strict` to verify all docs build with no broken links or missing pages.
  </action>
  <verify>
    - All 8 example pages exist in `sdk/docs/examples/`
    - Each example page contains at least one code block with the relevant client method
    - `sdk/README.md` has install instructions, quick start example, and docs link
    - `cd sdk && uv run mkdocs build --strict` succeeds with zero warnings
  </verify>
  <done>All endpoint categories have code example pages. README expanded for PyPI. Full docs site builds cleanly with --strict (no broken links).</done>
</task>

</tasks>

<verification>
1. `cd sdk && uv run mkdocs build --strict` completes with zero warnings/errors
2. All nav entries in mkdocs.yml resolve to existing files
3. Getting Started guide contains installation + first query + DataFrame example
4. Authentication guide covers direct key, from_env, context manager, error handling
5. Every client method (get_orderbook, list_markets, get_market, get_candles, list_events, get_event, list_deltas, list_trades, list_settlements, get_settlement) has at least one code example
6. README.md has install, quickstart, features, and docs link
</verification>

<success_criteria>
- mkdocs build --strict passes with all docs content present
- DOCS-01 satisfied: Getting Started guide with install + first query
- DOCS-02 satisfied: Authentication guide with all auth methods
- DOCS-04 satisfied: Code examples for every endpoint category
- README ready for PyPI rendering
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-pypi-publishing/12-02-SUMMARY.md`
</output>
